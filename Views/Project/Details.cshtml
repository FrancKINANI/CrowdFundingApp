@model Project

@{
    ViewData["Title"] = "Details of the project";
}

<h2>Details of the project</h2>

<div>
    <h4>Project</h4>
    <hr />
    <dl class="row">            
        <dt class="col-sm-2">Title</dt>
        <dd class="col-sm-10">@Model.Title</dd>
        <dt class="col-sm-2">Description</dt>
        <dd class="col-sm-10">@Model.Description</dd>
        <dt class="col-sm-2">Goal amount</dt>
        <dd class="col-sm-10">@Model.GoalAmount</dd>
        <dt class="col-sm-2">Current amount</dt>
        <dd class="col-sm-10">@Model.CurrentAmount</dd>
        <dt class="col-sm-2">Start date</dt>
        <dd class="col-sm-10">@Model.StartDate.ToString("yyyy-MM-dd")</dd>
        <dt class="col-sm-2">End date</dt>
        <dd class="col-sm-10">@Model.EndDate.ToString("yyyy-MM-dd")</dd>
        <dt class="col-sm-2">Creator</dt>
        <dd class="col-sm-10">@Model.User?.UserName</dd>
        <dt class="col-sm-2">Category</dt>
        <dd class="col-sm-10">@Model.Category?.Name</dd>
    </dl>
</div>
<div>
    <a asp-controller="Contribution" asp-action="Create" asp-route-id="@Model.ProjectId" class="btn btn-info btn-sm">Contribute</a>
    <a asp-action="Edit" asp-route-id="@Model.ProjectId" class="btn btn-warning btn-sm">Edit</a> |
    <a asp-action="Index" class="btn btn-sm">Back to the list</a>
</div>
